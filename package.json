{
    "name": "bun-image-optimizer",
    "module": "src/index.ts",
    "type": "module",
    "scripts": {
        "build": "bun build src/index.ts --outdir dist --target bun --minify",
        "compile": "bun build src/index.ts --outfile dist/bun-image-optimizer --target bun --minify --compile",
        "start": "bun dist/index.js",
        "lint": "eslint --max-warnings=0",
        "format": "prettier --write --config prettier.config.json .",
        "dev": "bun --watch src/index.ts",
        "test": "bun run test:unit && bun run test:integration",
        "test:unit": "bun test ./test/unit",
        "test:unit:dev": "bun test ./test/unit --watch",
        "test:integration": "bun test --preload ./test/integration/setup.ts test/integration",
        "test:integration:dev": "bun test --preload ./test/integration/setup.ts --watch test/integration",
        "compose:up": "docker compose up -d",
        "compose:down": "docker compose down",
        "db:migrate": "drizzle-kit migrate",
        "db:push": "drizzle-kit push",
        "db:studio": "drizzle-kit studio"
    },
    "dependencies": {
        "drizzle-orm": "^0.45.2",
        "superjson": "^2.2.6",
        "zod": "^4.5.0"
    },
    "devDependencies": {
        "@eslint/js": "^10.0.1",
        "@testcontainers/postgresql": "^12.1.0",
        "@testcontainers/redis": "^12.1.0",
        "@types/bun": "latest",
        "@types/node": "^25.9.1",
        "drizzle-kit": "^0.31.10",
        "eslint": "^10.6.0",
        "globals": "^17.7.0",
        "jiti": "^2.7.0",
        "prettier": "^3.9.1",
        "prettier-plugin-organize-imports": "^4.3.0",
        "typescript-eslint": "^8.62.0"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "packageManager": "bun@1.4.0"
}
